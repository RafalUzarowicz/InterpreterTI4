int factorial( int n ){
    if( n <= 1 ){
        return 1;
    }
    return n*factorial(n-1);
}

int main(){
    int[] arr = int[7]{0, 1, 2, 3, 4, 5, 6};
    string str = "tak";
    print("tak ", 2, str);
    foreach( var x : arr ){
            print(factorial(x));
    }
    return 0;
}